\documentclass[oneside,a4paper,14pt]{extarticle}
\usepackage[a4paper,top=20mm,bottom=20mm,left=20mm,right=10mm]{geometry}
\emergencystretch=3em
\usepackage[russian]{babel}
\usepackage{indentfirst}
\usepackage{graphicx}
\usepackage{caption}
\usepackage{titlesec}
\usepackage{minted, fancyvrb}
\usepackage{hyperref}
\usepackage{enumitem}
\usepackage{float}

% Code styling
\setminted{style = rainbow_dash, fontsize = \small, breaklines}

% Header styles
\titleformat{\section}{\normalsize\bfseries}{\thesection}{1em}{}
\titleformat{\subsection}{\normalsize\bfseries}{\thesubsection}{1em}{}
\titleformat{\subsubsection}{\normalsize\bfseries}{\thesubsubsection}{1em}{}

% Spacing
\renewcommand\baselinestretch{1.33}
\setlength{\parindent}{1.25cm}

% List styles
\setlist[enumerate]{
  left=\parindent,
  label=\arabic*.,
  itemsep=0pt,
  topsep=5pt,
  partopsep=0pt,
  parsep=0pt
}
\setlist[itemize]{
  left=\parindent,
  itemsep=0pt,
  topsep=5pt,
  partopsep=0pt,
  parsep=0pt
}

% Hyperlinks
\hypersetup{
  colorlinks=true,
  linkcolor=black,
  urlcolor=blue,
  pdfborder={0 0 0},
  pdftitle={Система управления гоночными командами — Лабораторная работа 2},
  pdfauthor={Черкасов А.А.}
}

\begin{document}

\newpage\thispagestyle{empty}
\begin{center}
  \MakeUppercase{
    Министерство науки и высшего образования Российской Федерации\\
    Федеральное государственное бюджетное образовательное учреждение высшего образования\\
    <<Вятский Государственный Университет>>\\
  }
  Институт математики и информационных систем\\
  Факультет автоматики и вычислительной техники\\
  Кафедра электронных вычислительных машин
\end{center}
\vfill

\begin{center}
  Отчет по лабораторной работе №2\\
  по дисциплине\\
  <<Объектно-ориентированное программирование>>\\
\end{center}
\vfill

\noindent
\begin{tabular}{ll}
  Выполнил студент гр. ИВТб-2301-05-00 \hspace{5mm} & \rule[-1mm]{25mm}{0.10mm}\,/Черкасов А. А./ \\
  Руководитель преподаватель                        & \rule[-1mm]{25mm}{0.10mm}\,/Шмакова Н. А./ \\
\end{tabular}

\vfill
\begin{center}
  Киров 2026
\end{center}

\newpage
\section*{\hspace{12.5mm}Цель работы}
Цель работы: изучить и освоить принципы объектно-ориентированного программирования, включая инкапсуляцию, наследование и полиморфизм, путем создания иерархии классов в выбранной предметной области (управление гоночными командами), взаимодействия с базой данных и графическим веб-интерфейсом.

\section*{\hspace{12.5mm}Задание}
На основании согласованной в первой лабораторной работе предметной области разработать приложение с графическим интерфейсом пользователя, используя классы и особенности C\#. Сведения об объектах предметной области необходимо хранить в БД.

\section*{\hspace{12.5mm}Решение}
Предметная область описывает управление гоночными командами трех различных серий (Formula 1, Formula 2, Formula E). Система позволяет хранить информацию о командах и их гонщиках, учитывать их специфические характеристики, проводить симуляцию гонок с начислением очков гонщикам и командам по правилам конкретной серии, выполнять сравнение команд по очкам через перегруженные операторы и выводить информацию о них в удобном виде.

Программа содержит классы <<FormulaTeam>>, представляющий базовый абстрактный класс команды, <<F1Team>>, наследованный от <<FormulaTeam>> и представляющий класс команды Formula 1, <<F2Team>>, наследованный от <<FormulaTeam>> и представляющий класс команды Formula 2, <<FormulaETeam>>, наследованный от <<FormulaTeam>> и представляющий класс команды Formula E, а также вспомогательный класс <<FormulaDriver>> для хранения информации о гонщиках.

Базовый класс <<FormulaTeam>> содержит:
\begin{itemize}
  \item[--] Свойства:
  \begin{itemize}
    \item[--] public int Id \{ get; set; \} -- свойство, уникальный идентификатор;
    \item[--] public string TeamName \{ get; set; \} -- свойство, название команды;
    \item[--] public string PrincipalName \{ get; set; \} -- свойство, имя руководителя команды;
    \item[--] public string Headquarters \{ get; set; \} -- свойство, штаб-квартира команды;
    \item[--] public string? TeamColor \{ get; set; \} -- свойство, цвет команды;
    \item[--] public int ChampionshipPoints \{ get; set; \} -- свойство, очки чемпионата;
    \item[--] public int RaceWins \{ get; set; \} -- свойство, количество побед;
    \item[--] public int Podiums \{ get; set; \} -- свойство, количество подиумов;
    \item[--] public List<FormulaDriver> Drivers \{ get; set; \} -- свойство, список гонщиков;
  \end{itemize}
  \item[--] Конструкторы:
  \begin{itemize}
    \item[--] protected FormulaTeam() -- защищенный конструктор по умолчанию;
    \item[--] protected FormulaTeam(string name, string principal, string hq) -- защищенный конструктор, принимает название, имя руководителя и штаб-квартиру;
    \item[--] protected FormulaTeam(string name, string principal, string hq, int points, int wins, int podiums) -- защищенный конструктор, принимает все основные параметры;
  \end{itemize}
  \item[--] Абстрактные методы:
  \begin{itemize}
    \item[--] public abstract string GetSeriesName() -- публичный абстрактный метод, возвращающий название серии;
    \item[--] public abstract int CalculatePoints(int position) -- публичный абстрактный метод, возвращающий очки за занятую позицию;
  \end{itemize}
  \item[--] Перегруженные операторы:
  \begin{itemize}
    \item[--] public static bool operator >(FormulaTeam a, FormulaTeam b) -- оператор больше, сравнивает очки команд;
    \item[--] public static bool operator <(FormulaTeam a, FormulaTeam b) -- оператор меньше, сравнивает очки команд;
  \end{itemize}
  \item[--] Собственные методы:
  \begin{itemize}
    \item[--] public void AddPoints(int pts) -- публичный метод, добавляет очки команде;
    \item[--] public void AddWin() -- публичный метод, увеличивает количество побед и подиумов;
    \item[--] public void AddPodium() -- публичный метод, увеличивает количество подиумов;
    \item[--] public double GetWinRate(int totalRaces) -- публичный метод, рассчитывает процент побед;
    \item[--] public virtual string GetSummary() -- публичный виртуальный метод, возвращает сводку по команде.
  \end{itemize}
\end{itemize}

Класс <<F1Team>>, наследованный от класса <<FormulaTeam>> содержит:
\begin{itemize}
  \item[--] Собственные свойства:
  \begin{itemize}
    \item[--] public string PowerUnit \{ get; set; \} -- свойство, тип силовой установки;
    \item[--] public double BudgetCapMln \{ get; set; \} -- свойство, лимит бюджета в миллионах;
    \item[--] public int ConstructorPos \{ get; set; \} -- свойство, позиция в кубке конструкторов;
  \end{itemize}
  \item[--] Конструкторы:
  \begin{itemize}
    \item[--] public F1Team() : base() -- публичный конструктор по умолчанию;
    \item[--] public F1Team(string name, string principal, string hq, string powerUnit) : base(name, principal, hq) -- конструктор, принимающий основные параметры;
    \item[--] public F1Team(string name, string principal, string hq, string powerUnit, double budget, int ctorPos, int points, int wins, int podiums) : base(name, principal, hq, points, wins, podiums) -- конструктор, принимающий все параметры;
  \end{itemize}
  \item[--] Переопределенные методы:
  \begin{itemize}
    \item[--] public override string GetSeriesName() -- возвращает <<Formula 1>>;
    \item[--] public override int CalculatePoints(int position) -- возвращает очки по системе F1 (25-18-15-12-10-8-6-4-2-1);
    \item[--] public override string GetSummary() -- возвращает сводку с учетом силовой установки и бюджета;
  \end{itemize}
  \item[--] Собственные методы:
  \begin{itemize}
    \item[--] public double CalculateBudgetAllocation(double percentage) -- метод расчета выделения бюджета;
    \item[--] public string GetPowerUnitStatus() -- возвращает статус силовой установки (Dominant, Competitive, Development).
  \end{itemize}
\end{itemize}

Класс <<F2Team>>, наследованный от класса <<FormulaTeam>> содержит:
\begin{itemize}
  \item[--] Собственные свойства:
  \begin{itemize}
    \item[--] public string ChassisModel \{ get; set; \} -- свойство, модель шасси;
    \item[--] public int F1Graduates \{ get; set; \} -- свойство, число выпускников, перешедших в F1;
    \item[--] public bool IsFeederSeries \{ get; set; \} -- свойство, является ли молодежной серией;
  \end{itemize}
  \item[--] Конструкторы:
  \begin{itemize}
    \item[--] public F2Team() : base() -- конструктор по умолчанию;
    \item[--] public F2Team(string name, string principal, string hq, string chassis) : base(name, principal, hq) -- конструктор, принимающий основные параметры;
    \item[--] public F2Team(string name, string principal, string hq, string chassis, int graduates, bool feeder, int points, int wins, int podiums) : base(name, principal, hq, points, wins, podiums) -- конструктор, принимающий все параметры;
  \end{itemize}
  \item[--] Переопределенные методы:
  \begin{itemize}
    \item[--] public override string GetSeriesName() -- возвращает <<Formula 2>>;
    \item[--] public override int CalculatePoints(int position) -- возвращает очки по системе F2 (15-12-10-8-6-5-4-3-2-1);
    \item[--] public override string GetSummary() -- возвращает сводку с учетом модели шасси и числа выпускников;
    \item[--] public string GetFeederStatus() -- возвращает статус молодежной команды.
  \end{itemize}
\end{itemize}

Класс <<FormulaETeam>>, наследованный от класса <<FormulaTeam>> содержит:
\begin{itemize}
  \item[--] Собственные свойства:
  \begin{itemize}
    \item[--] public int SustainabilityScore \{ get; set; \} -- свойство, рейтинг экологичности;
    \item[--] public double BatteryCapacityKwh \{ get; set; \} -- свойство, емкость батареи;
    \item[--] public string EnergyPartner \{ get; set; \} -- свойство, энергетический партнер;
  \end{itemize}
  \item[--] Конструкторы:
  \begin{itemize}
    \item[--] public FormulaETeam() : base() -- конструктор по умолчанию;
    \item[--] public FormulaETeam(string name, string principal, string hq, string energyPartner) : base(name, principal, hq) -- конструктор с основными параметрами;
    \item[--] public FormulaETeam(string name, string principal, string hq, string energyPartner, int sustainScore, double batteryKwh, int points, int wins, int podiums) : base(name, principal, hq, points, wins, podiums) -- конструктор со всеми параметрами;
  \end{itemize}
  \item[--] Переопределенные методы:
  \begin{itemize}
    \item[--] public override string GetSeriesName() -- возвращает <<Formula E>>;
    \item[--] public override int CalculatePoints(int position) -- возвращает очки по системе Formula E;
    \item[--] public override string GetSummary() -- возвращает сводку с учетом экологичности и емкости батареи;
  \end{itemize}
  \item[--] Собственные методы:
  \begin{itemize}
    \item[--] public string GetBatteryEfficiencyRating() -- возвращает рейтинг эффективности батареи.
  \end{itemize}
\end{itemize}

Класс <<FormulaDriver>> представляет гонщика и содержит:
\begin{itemize}
  \item[--] Свойства:
  \begin{itemize}
    \item[--] public int Id \{ get; set; \} -- свойство, уникальный идентификатор;
    \item[--] public int TeamId \{ get; set; \} -- свойство, идентификатор команды (внешний ключ);
    \item[--] public string FirstName \{ get; set; \} -- свойство, имя гонщика;
    \item[--] public string LastName \{ get; set; \} -- свойство, фамилия гонщика;
    \item[--] public int RacingNumber \{ get; set; \} -- свойство, гоночный номер;
    \item[--] public int Points \{ get; set; \} -- свойство, набранные очки;
  \end{itemize}
  \item[--] Конструкторы:
  \begin{itemize}
    \item[--] public FormulaDriver() -- публичный конструктор по умолчанию;
    \item[--] public FormulaDriver(string firstName, string lastName, int number, int points = 0) -- параметризованный конструктор;
  \end{itemize}
  \item[--] Собственные методы:
  \begin{itemize}
    \item[--] public void AddPoints(int pts) -- увеличивает количество набранных очков;
  \end{itemize}
  \item[--] Вычисляемые свойства:
  \begin{itemize}
    \item[--] public string FullName \{ get; \} -- возвращает полное имя гонщика;
    \item[--] public string DisplayString \{ get; \} -- возвращает строку отображения пилота.
  \end{itemize}
\end{itemize}

Диаграмма классов сущностей представлена на рисунке 1.

\pagebreak
\begin{figure}[ht]
  \centering
  \includegraphics[width=0.95\linewidth,height=0.45\textheight,keepaspectratio]{pics/uml-1.png}
\end{figure}
\begin{center}
  Рисунок 1 – Диаграмма классов сущностей
\end{center}

Разработаны серверное приложение на бэкенде в виде REST API на C\# и клиентское приложение в виде веб-интерфейса (React). Серверное приложение содержит подключение к базе данных PostgreSQL, конфигурацию моделей с использованием TPH и маршруты API.

Подключение к базе данных происходит с помощью следующей строки подключения в методе OnConfiguring класса RacingDbContext:
\begin{minted}{csharp}
optionsBuilder.UseNpgsql(
    "Host=localhost;Database=postgres;" +
    "Username=postgres;Password=postgres");
\end{minted}

И сам класс контекста:
\begin{minted}{csharp}
public class RacingDbContext : DbContext
{
    public DbSet<FormulaTeam> Teams { get; set; } = null!;
    public DbSet<FormulaDriver> Drivers { get; set; } = null!;

    protected override void OnConfiguring(
        DbContextOptionsBuilder optionsBuilder)
    {
        optionsBuilder.UseNpgsql(
            "Host=localhost;Database=postgres;" +
            "Username=postgres;Password=postgres");
    }

    protected override void OnModelCreating(ModelBuilder modelBuilder)
    {
        // Настройка TPH и связей
        base.OnModelCreating(modelBuilder);
    }
}
\end{minted}

Конфигурация сущности FormulaTeam с использованием стратегии Table-per-Hierarchy (TPH) содержит настройку дискриминатора по полю series\_id для деления на F1, F2 и Formula E:
\begin{minted}{csharp}
modelBuilder.Entity<FormulaTeam>()
    .ToTable("racing_teams")
    .HasDiscriminator<long>("series_id")
    .HasValue<F1Team>(1)
    .HasValue<F2Team>(2)
    .HasValue<FormulaETeam>(3);
\end{minted}

Следующим шагом является маршрутизация API-запросов от клиентов. Обработчик ProcessRequest в классе Program выполняет роль маршрутизатора API.

Содержит следующие методы:
\begin{itemize}
  \item[--] GET /api/teams -- возвращает список всех команд с их гонщиками, отсортированных по очкам;
  \item[--] POST /api/teams -- создает новую команду;
  \item[--] PUT /api/teams/\{id\} -- обновляет существующую команду;
  \item[--] DELETE /api/teams/\{id\} -- удаляет команду и каскадно удаляет привязанных к ней гонщиков;
  \item[--] POST /api/teams/\{id\}/simulate-race -- симулирует гонку для команды, полиморфно вычисляя очки по занятому месту и сохраняя изменения в базе данных:
\begin{minted}{csharp}
int points = team.CalculatePoints(position);
driver.AddPoints(points);
team.AddPoints(points);
\end{minted}
  \item[--] GET /api/drivers -- возвращает список всех гонщиков;
  \item[--] POST /api/drivers -- создает запись о гонщике;
  \item[--] PUT /api/drivers/\{id\} -- обновляет запись о гонщике;
  \item[--] DELETE /api/drivers/\{id\} -- удаляет запись о гонщике по идентификатору;
  \item[--] GET /api/teams/compare -- метод сравнения двух команд по очкам с помощью перегруженных операторов:
\begin{minted}{csharp}
public static bool operator >(FormulaTeam a, FormulaTeam b) 
    => a.ChampionshipPoints > b.ChampionshipPoints;
\end{minted}
\end{itemize}

Диаграмма классов контроллеров и конфигураций сущностей представлены на рисунке 2.

\begin{figure}[ht]
  \centering
  \includegraphics[width=0.95\linewidth,height=0.42\textheight,keepaspectratio]{pics/uml-2.png}
\end{figure}
\begin{center}
  Рисунок 2 – Диаграмма классов базы данных и API
\end{center}

Также разработан веб-интерфейс для взаимодействия с WEB API, экранные формы которого представлены на рисунках 3, 4, 5 и 6.

\pagebreak
\begin{figure}[ht]
  \centering
  \includegraphics[width=0.95\linewidth,height=0.35\textheight,keepaspectratio]{pics/form-1.png}
\end{figure}
\begin{center}
  Рисунок 3 – Экранная форма вкладки команд (standings)
\end{center}

\begin{figure}[ht]
  \centering
  \includegraphics[width=0.95\linewidth,height=0.35\textheight,keepaspectratio]{pics/form-2.png}
\end{figure}
\begin{center}
  Рисунок 4 – Экранная форма вкладки гонщиков
\end{center}

\pagebreak
\begin{figure}[ht]
  \centering
  \includegraphics[width=0.95\linewidth,height=0.35\textheight,keepaspectratio]{pics/form-3.png}
\end{figure}
\begin{center}
  Рисунок 5 – Экранная форма создания и редактирования команд
\end{center}

\begin{figure}[ht]
  \centering
  \includegraphics[width=0.95\linewidth,height=0.35\textheight,keepaspectratio]{pics/form-4.png}
\end{figure}
\begin{center}
  Рисунок 6 – Экранная форма создания и редактирования гонщиков
\end{center}

\section*{\hspace{12.5mm}Вывод}
В ходе выполнения лабораторной работы была разработана и реализована на языке C\# иерархия классов, моделирующая управление гоночными командами различных серий. Изучены основные принципы объектно-ориентированного программирования: инкапсуляция, наследование, полиморфизм. Реализованы механизмы работы с реляционной базой данных PostgreSQL через Entity Framework Core с использованием стратегии Table-per-Hierarchy (TPH). Разработано веб-приложение на базе React и бэкенда на C\# REST API, демонстрирующее корректное применение данных принципов.

\newpage

\section*{Приложение А1. Базовый класс FormulaTeam.cs}
\inputminted{csharp}{code/src/Core/FormulaTeam.cs}

\section*{Приложение А2. Класс F1Team.cs}
\inputminted{csharp}{code/src/Core/F1Team.cs}

\section*{Приложение А3. Класс F2Team.cs}
\inputminted{csharp}{code/src/Core/F2Team.cs}

\section*{Приложение А4. Класс FormulaETeam.cs}
\inputminted{csharp}{code/src/Core/FormulaETeam.cs}

\section*{Приложение А5. Класс FormulaDriver.cs}
\inputminted{csharp}{code/src/Core/FormulaDriver.cs}

\end{document}
